{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Debug Server",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${workspaceFolder}/build/src/server.js",
            "envFile": "${workspaceFolder}/.env.debug",
            "outFiles": [
                "${workspaceFolder}/**/*.js"
            ]
        }
    ]
}
